# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (generated by `expo prebuild` — local Expo modules under modules/ are tracked)
/ios
/android
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# TypeScript
*.tsbuildinfo

# Testing
coverage/

# macOS
.DS_Store
*.pem

# Local env
.env*.local

# Legacy iOS (Xcode) build artifacts — source is kept for reference under legacy-ios/
legacy-ios/**/xcuserdata/
legacy-ios/**/build/
legacy-ios/**/DerivedData/
legacy-ios/**/*.xcuserstate

# Local Expo module Android build artifacts (generated)
modules/*/android/build/

# Local secrets (real AdMob/RevenueCat keys) — Expo auto-loads .env; never commit
.env
.env.local

# Competitor reference screenshots (local study only)
examples-apps-imgs/
